Currently in Progress!

Working On: 
Adding ML models to compare GLMs against
Adding Gini score and lift charts
Finished:
Poisson GLM for frequency model
Gamma GLM for Severity model
evaluation metrics for both GLMs


freMTPL2freq.csv & freMTPL2sev.csv are the original datasets.
combined_data.csv combines the original datasets which is then used for training and testing the models.
